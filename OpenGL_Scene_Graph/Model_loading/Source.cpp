
#include"scene.h"

int main(int argc, char** argv)
{

	scene scn;
	
	scn.run(argc,argv);
	//	Mouse and Keyboard Callbacks
	//glutKeyboardFunc(scn.processKeys);
	//glutMouseFunc(scn.processMouseButtons);
	//glutMotionFunc(scn.processMouseMotion);

	//glutDisplayFunc(&scn.displaywrapper);
	//glutMainLoop();

}